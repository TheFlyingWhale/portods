import React from "react";
import { Button, Heading, Input } from "./components";

const App = () => {
    return (
        <>
            <Heading>This is a heading</Heading>
            <Button>This is a button</Button>
            <Input placeholder="some placeholder text" />
        </>
    );
};

export default App;
